import 'package:flutter/material.dart';

class WindowsIndicator extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO :: build Progress Indicator for windwos
    return Container(
      color: Colors.black54,
      child: Center(
        child: Text('still in progress'),
      ),
    );
  }
}
